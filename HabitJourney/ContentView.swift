// Página para renderizar as telas

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
//           SplashScreen();
            HomeScreen();
            Spacer()
        }
    }
}

#Preview {
    ContentView()
}
