// UIFile responsável pela paleta de cores da aplicação - TO-DO definir as cores de todas telas (FIGMA - VARIABLES)

import SwiftUI

struct AppColors {

  // TaskMain com diferentes opacidades
  static let taskMain750 = Color(hex: "#093631") // Com 100% opacidade
  
  static let taskMain750 = Color(hex: "#093631BF") // Com 75% opacidade

  static let taskMain550 = Color(hex: "#0936318C") // Com 55% opacidade

  static let taskMain350 = Color(hex: "#09363159") // Com 35% opacidade

  static let taskMain200 = Color(hex: "#09363133") // Com 20% opacidade

  // AppMain (Botões e Input)
  static let appMain = Color(hex: "#1B998B")

  // MarginMain para Bolinhas e Input
  static let marginMain = Color(hex: "#03110F")
  static let marginSecondary = Color(hex: "#98D1CB")
  static let marginLogo = Color(hex: "#656176")

  // Cor de plano de fundo
  static let backgroundMain = Color(hex: "#FCF9FF")
}
